[var를 사용한 변수 선언]
- 변수 선언은 런타임(소스코드가 한 줄씩 순차적으로 실행되는 시점)이 아닌, 그 이전 단계에서 먼저 실행됨
- 선언 단계와 초기화 단계가 동시에 진행




var score;

- 선언 단계를 통해 변수 이름 score를 등록
- 초기화 단꼐를 통해 score 변수에 암묵적으로 undefined를 할당해 초기화
- 즉, var로 선언한 변수는 어떠한 값을 할당하지 않아도 undefined를 가짐


